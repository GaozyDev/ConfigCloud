spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/SpringCloud_Sell/config-repo
          username:
          password:
          basedir:
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/